import movimiento.*
import wollok.game.*
import chefs.*
import ingredientes.*
import escenario.*
import menu.*
import clientes.*
import nivel.*

describe "pruebas" {
  test "description" {
    generarPedido.pedido1(menuJoven1)
    generarPedido.pedido2(menuJoven2)
    generarPedido.pedido3(menuJoven3)
    assert.equals(#{menuJoven3}, generarPedido.pedidosDisponibles())
  }
}